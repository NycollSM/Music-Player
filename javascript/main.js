(function () {
    let player = null;

    function main() {
       // player = new Singleton(songsArray);
        //let prueba = player.songsArray;
        //console.log(prueba);
       // playlist();
       
       // console.log('playlist', player);   
    }

    // waits  for the DOM to be ready
    document.addEventListener('DOMContentLoaded', main);
})(); 